/**
 * HTTP API routes module.
 *
 * Exports all route registration functions for the daemon server.
 *
 * @module daemon/routes
 */

export { sessionRoutes } from "./sessions";
export { groupRoutes } from "./groups";
export { queueRoutes } from "./queues";
export { bookmarkRoutes } from "./bookmarks";
